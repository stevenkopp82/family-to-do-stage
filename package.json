{
  "name": "family-todo-tests",
  "version": "1.0.0",
  "description": "Unit tests for Family To-Do logic functions",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch",
    "cap:prepare": "mkdir -p www && cp index.html app.js style.css www/",
    "cap:sync": "npm run cap:prepare && npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.2.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "@capacitor-firebase/authentication": "^8.1.0",
    "@capacitor/android": "^8.2.0",
    "@capacitor/core": "^8.2.0"
  }
}
